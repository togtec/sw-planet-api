# Schema Initialization
spring.jpa.hibernate.ddl-auto=update

# Database
spring.datasource.url=${MYSQL_HOST:jdbc:mysql://localhost:3306/starwars}
# spring.datasource.url=jdbc:tc:mysql:8.0:///db?TC_IMAGE_TAG=8.0
spring.datasource.username=${MYSQL_USERNAME:starwars_dev}
spring.datasource.password=${MYSQL_PASSWORD:admin}